package com.jetbrains;

// Pre Lab P2
class PreTwo {
    public char MakeItBig(char word) {
        Character.toUpperCase(word);
        return word;
    }

    public char MakeItSmall(char word) {
        Character.toLowerCase(word);
        return word;
    }
}
